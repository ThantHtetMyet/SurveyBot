API
----
curl -i http://localhost:5001/api/auth/signin

curl -i -X POST http://192.3.71.120:5001/api/auth/signin ^
   -H "Content-Type: application/json" ^
   -d "{\"email\":\"system@gmail.com\",\"password\":\"12345\"}"

dotnet run --urls "http://0.0.0.0:5000;https://localhost:7145"

dotnet ef migrations add AddServerHostNameWarehouseTable
dotnet ef database update

dotnet publish "C:\Users\thanthtet.myet\Documents\01_Willowglen\B_001_Workplace\SurveyBot\survey-bot-api\survey-bot-api\survey-bot-api.csproj" -c Release -o "C:\Publish\Survey-Bot-API"


Git
-------
# 1. Switch to the main branch
git checkout main
# Now you are on the main branch

# 2. Update main to the latest version from remote
git pull origin main
# This ensures your local main branch is up-to-date with remote
git reset --hard HEAD
git clean -fd
# 3. Merge changes from development branch into main
git merge development
# This brings all updates from development into main
# Resolve any merge conflicts if Git reports them

# 4. Push the updated main branch to remote
git push origin main
# Now the remote main branch has all the updates from development


git remote -v

git remote set-url origin https://github.com/ThantHtetMyet/Control-Tower-Python.git

# To refresh for created new branches
git fetch --all
git branch -a

WEB
--------
npm install --save react-organizational-chart
npm install @mui/x-date-pickers date-fns